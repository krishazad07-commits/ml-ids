# Additional packages for the training notebooks in notebooks/.
# Not required to run the API — install with:
#   pip install -r requirements.txt -r requirements-dev.txt

jupyter==1.1.1
matplotlib==3.10.9
seaborn==0.13.2